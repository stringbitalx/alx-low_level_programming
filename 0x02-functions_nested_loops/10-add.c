#include "main.h"

/**
 * add - adds two numbers
 * @i: first number
 * @j: second number
 *
 * Return: the sum of the two numbers.
 */

int add(int i, int j)
{
	int sum;

	sum = (i + j);

	return (sum);
}
