#include "main.h"

/**
 * mul - multiplies two numbers
 * @a: digit to be multiplied
 * @b: digit to be multiplied
 * Return: 0 success
 */

int mul(int a, int b)
{
	int multiple = a * b;

	return (multiple);
}
