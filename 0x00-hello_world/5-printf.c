#include <stdio.h>
/**
 * main - prints text
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char a;

	a = "with proper grammar, but the outcome is a piece of art,"

	printf("%a", a);
	return (0);
}
