#include <stdio.h>
/**
 * main - prints text
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char a[55] = "with proper grammar, but the outcome is a piece of art,";

	printf("%c", a);
	return (0);
}
