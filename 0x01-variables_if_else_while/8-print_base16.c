#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main  - printhexadecimal numbers in lowercas
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int a;

	for (a = 61; a < 7A; a++)
	{
		putchar(a);
	}

	putchar('\n');
	return (0);
}
