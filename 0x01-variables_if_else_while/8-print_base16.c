#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main  - printhexadecimal numbers in lowercas
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int a;

	for (a = 061; a < 07A; a++)
	{
		putchar(a);
	}

	putchar('\n');
	return (0);
}
